import { Meta,  IconGallery, IconItem } from '@storybook/addon-docs';

import SVG_ICONS from "@assets/svgs";
import SvgIcon from '@components/SvgIcons/SvgIcon';

import GlobalThemeProvider from "@themes/GlobalThemeProvider";


<Meta title="Components/SvgIcons/Gallery" />

<GlobalThemeProvider>
  <IconGallery>
    {Object.keys(SVG_ICONS).map((SVG_ICON) =>
      <IconItem name={SVG_ICON}>
        <SvgIcon icon={SVG_ICON} />
      </IconItem>
    )}
  </IconGallery>
</GlobalThemeProvider>