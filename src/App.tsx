import Tooltip from "@components/Tooltips/Tooltip";

const App = () => {
  return (
    <div>
      <Tooltip text="text" isArrow>
        <textarea />
      </Tooltip>
    </div>
  );
};

export default App;
